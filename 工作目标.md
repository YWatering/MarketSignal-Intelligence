# MarketSignal Intelligence Agent Skill 工作目标

## 1. Skill 名称

**MarketSignal Intelligence**

建议的 Skill 标识：`market-signal-intelligence`

该名称强调对市场信号的综合识别与分析，覆盖行情、财务、新闻、网络舆情、Excel 数据交付和股价预测等核心能力。

## 2. 项目定位

本项目旨在构建一个面向网络舆情、新闻、财务与证券行情分析的 Agent Skill。Skill 接收用户指定的股票、行业、公司或主题，以及时间范围、市场范围和分析要求，自动完成数据获取、清洗、标准化、分析、Excel 文档生成和股价预测分析。

项目的核心目标不是简单返回零散数据，而是形成一条可复现、可追溯、可解释的研究工作流：

> 数据采集 → 数据治理 → 指标计算 → 多维分析 → Excel 交付 → 预测评估 → 结论与风险说明

## 3. 总体目标

### 3.1 数据获取目标

- 支持按股票代码、公司名称、行业、关键词和主题进行数据检索。
- 获取与目标对象相关的网络舆情、新闻、公告、财务指标、历史股价、成交量及其他核心市场指标。
- 优先使用稳定、合法、可持续的公开 API 或结构化数据源；在确需爬取时遵守目标站点的服务条款、robots.txt、访问频率限制和版权要求。
- 保存每条数据的来源、抓取时间、发布时间、数据周期、原始链接和必要的版本信息。
- 支持失败重试、超时处理、限流、分页、去重和断点续取，减少因单一数据源异常导致的整体任务失败。

### 3.2 数据处理目标

- 建立统一的数据模型，至少覆盖：主体信息、行情数据、财务数据、新闻数据、舆情数据、衍生指标和预测结果。
- 处理缺失值、重复记录、异常值、字段类型错误、时区差异、币种差异和公告/新闻时间与交易日不一致等问题。
- 统一股票代码、公司名称、行业分类、日期格式、货币单位、数值精度和指标命名。
- 对新闻和舆情文本进行去重、分词或关键词提取、主题归类、情感倾向分析和影响程度标注；所有自动判断都必须保留原文或可追溯依据。
- 区分原始数据、清洗数据、计算数据和人工修订数据，避免处理过程覆盖原始记录。

### 3.3 分析与指标目标

- 计算价格、收益率、波动率、成交量变化、均线、动量、回撤等常用行情指标。
- 计算收入、利润、现金流、资产负债、估值、盈利能力、偿债能力和成长性等财务指标；明确报告期、口径和单位。
- 分析新闻与舆情的数量趋势、主题分布、情感变化、热点事件、正负面来源和与行情变化的时间关系。
- 支持将基本面、技术面、舆情面和市场环境组合为可解释的分析框架。
- 对相关性、因果性和预测性进行严格区分，不将时间上的同步变化直接表述为因果关系。

### 3.4 Excel 文档交付目标

根据任务自动生成结构清晰、可复核、便于继续编辑的 Excel 文档。建议至少包含以下工作表：

1. `README`：任务参数、数据范围、数据源、免责声明和使用说明。
2. `主体信息`：股票或公司基本信息、行业、市场、币种和代码映射。
3. `行情数据`：日期、开高低收、成交量、成交额、涨跌幅和复权说明。
4. `财务数据`：报告期、指标名称、指标值、单位、口径和来源。
5. `新闻舆情`：发布时间、标题、来源、链接、关键词、主题、情感和影响标注。
6. `指标分析`：衍生指标、统计结果、趋势判断和计算口径。
7. `预测结果`：预测区间、预测值、置信区间或不确定性、基准模型、模型版本和评估指标。
8. `数据质量`：缺失率、重复数、异常记录数、失败数据源和质量告警。

Excel 交付物应具备以下特征：

- 表头、单位、日期格式和数字格式统一。
- 关键字段可筛选、冻结表头，长文本和链接可正常查看。
- 对重要指标提供来源和计算说明。
- 对异常、缺失、预测和低置信度结果进行明确标记。
- 生成文件名包含主体和任务类型，并在需要时使用版本号避免覆盖历史结果。
- 在数据量较大时提供汇总表，同时保留明细数据或明细文件索引。

### 3.5 股价预测分析目标

- 在预测前明确预测对象、频率、预测周期、训练区间、特征字段和数据截止时间。
- 建立可比较的基准模型，例如上期值、移动平均或历史均值，避免只展示复杂模型结果。
- 根据数据规模和任务需要选择时间序列、回归或机器学习方法；模型选择必须考虑可解释性、稳定性和数据泄漏风险。
- 使用严格的时间序列切分、滚动验证或走步验证，禁止将未来数据泄漏到训练集或特征工程过程。
- 输出点预测与不确定性信息，并同时报告 MAE、RMSE、MAPE、方向准确率等适用评估指标。
- 展示预测与实际值对比、误差变化、特征重要性或影响因素，并说明模型适用范围和失效风险。
- 将预测结果定位为研究辅助信息，不作确定性涨跌承诺，不构成投资建议。

## 4. Skill 的主要使用场景

### 场景 A：单只股票综合分析

用户提供股票代码和时间范围，Skill 汇总行情、财务、新闻与舆情数据，生成综合分析 Excel，并输出重点变化、风险信号和预测结果。

### 场景 B：行业或主题舆情分析

用户提供行业或关键词，Skill 识别相关主体和事件，统计舆情热度、主题演变、情感变化及其与行业指数或个股行情的关系。

### 场景 C：多股票横向比较

用户提供股票列表，Skill 统一口径获取数据，生成估值、成长性、盈利能力、行情表现和舆情表现的对比结果。

### 场景 D：定期研究报告

Skill 按指定周期重复采集与分析，保留历史快照，比较本期与上期变化，并标记新增事件、指标拐点和数据质量变化。

## 5. 推荐的项目结构

后续实现可围绕以下结构逐步建设，实际目录以代码实现为准：

```text
shuishui/
├── SKILL.md                 # Skill 入口说明、触发条件与工作流
├── agents/
│   └── openai.yaml          # Skill 的界面元数据与调用配置
├── scripts/                 # 数据采集、清洗、分析、Excel 和预测脚本
├── references/              # 数据字典、指标口径、模型说明和数据源规范
├── assets/                  # Excel 模板、样式资源及其他必要资产
├── tests/                   # 单元测试、数据契约测试和端到端测试
└── outputs/                 # 本地生成的示例结果，不提交敏感或大体积数据
```

## 6. 核心设计原则

### 5.1 可追溯

所有重要结论都应能够追溯到原始数据、数据源、时间点、计算步骤和模型版本。对无法核实的信息明确标注，不用推测替代事实。

### 5.2 可复现

相同的输入参数、数据快照和配置应尽可能产生一致结果。记录运行参数、依赖版本、数据截止时间和随机种子。

### 5.3 数据先于结论

先进行数据范围、时效性、完整性和异常检查，再进行分析与预测。数据不足时，Skill 应主动降低结论强度或明确拒绝不可靠的预测任务。

### 5.4 解释优先

预测模型和综合评分应尽量提供可理解的计算依据、关键输入、主要限制和不确定性，不输出无法解释的单一结论。

### 5.5 安全与合规

- 遵守数据源的服务条款、访问限制、版权和隐私要求。
- 不采集、存储或输出不必要的个人敏感信息。
- 不绕过登录、验证码、付费墙或访问控制。
- 对财务预测、投资判断和舆情结论提供风险提示。
- 不把模型输出表述为保证收益、确定性预测或专业投资建议。

## 7. 验收标准

当以下条件全部满足时，可认为第一版 Skill 达到可用标准：

- 能根据自然语言任务识别主体、时间范围、市场和所需数据类型。
- 能获取并保存行情、财务、新闻或舆情数据的来源与时间信息。
- 能对主要数据执行字段标准化、去重、缺失和异常检查。
- 能生成可打开、可筛选、格式稳定且包含来源说明的 Excel 文件。
- 能输出基本行情、财务和舆情分析结果，并区分事实、统计结果和推断。
- 能使用至少一个基准模型完成预测，并报告验证方式和误差指标。
- 在数据不足、数据源失败、主体不明确或预测条件不成立时，能够给出清晰的错误信息或降级结果。
- 不出现明显的数据泄漏、来源缺失、单位混乱、日期错位或预测结果伪装成确定事实的问题。
- 代码、配置、文档和测试能够在新环境中按照项目说明复现。

## 8. 近期优先事项

1. 确定第一版支持的市场、股票代码格式、数据频率和时间范围。
2. 选择并记录合法、稳定、可持续的数据源及其字段映射。
3. 建立统一数据字典和最小输入/输出契约。
4. 完成 `SKILL.md` 与 Excel 最小模板。
5. 实现一个可重复运行的单股票端到端样例。
6. 用固定样例数据验证清洗、指标计算、Excel 输出和预测评估流程。

## 9. 项目完成后的预期成果

- 一个可被 Agent 发现和调用的完整 Skill。
- 一套可扩展的数据采集、处理、分析和预测脚本。
- 一套带来源、口径、质量信息和预测说明的 Excel 产出模板。
- 一套可复现的样例任务、测试数据和验收测试。
- 一份清晰说明适用范围、数据限制、预测风险和合规边界的项目文档。
