# MarketSignal Intelligence Agent Skill 工作计划

## 计划说明

本计划用于推进 `MarketSignal Intelligence` Skill 的开发，按数据采集、数据治理、分析交付、预测建模和稳定性建设逐步完成。每个阶段都应在完成前一阶段核心能力后推进，并通过可运行样例和测试结果验证成果。

## 阶段一：项目骨架与最小可用版本

### 完成状态

已完成并验证。

### 工作事项

- 完成 `SKILL.md`、调用元数据和基础目录。
- 明确输入参数、输出格式、数据字典和错误处理规范。
- 接入至少一种可稳定获取的行情数据源和一种新闻/舆情数据源。
- 实现基本数据清洗、去重、统一字段和 Excel 输出。
- 完成单只股票、单一时间范围的端到端演示。

### 完成方式

1. **Skill 骨架**：创建 `SKILL.md` 和 `agents/openai.yaml`，定义 `market-signal-intelligence` 的适用场景、阶段一能力边界、调用流程、依赖安装方式和命令入口；创建 `scripts/`、`references/`、`fixtures/`、`tests/` 与 `outputs/` 目录。
2. **输入输出契约**：在 `references/data_contract.md` 定义单股票任务的 `symbol`、时间范围、运行模式和输出路径；定义标准化行情与新闻记录，以及 `README`、`行情数据`、`新闻舆情`、`数据质量` 四个 Excel 工作表和错误处理规则。
3. **数据源接入**：在 `scripts/marketsignal.py` 实现 Twelve Data 日线行情适配器与 Yahoo Finance RSS 新闻适配器。在线模式要求 Twelve Data API Key，并在缺失凭据、网络错误、源数据格式异常或无有效记录时明确失败。`fixture` 模式使用仓库内固定数据，确保离线可重复执行。
4. **清洗与 Excel 导出**：实现行情按交易日去重、数值与高低价有效性校验、新闻按链接或标题与发布时间去重、缺失标题剔除、请求范围过滤、统一字段和排序。使用 `openpyxl` 生成带筛选、冻结表头、数值格式、链接和数据质量统计的 Excel；导出后移除 OOXML 核心属性。
5. **单股票演示**：通过 `scripts/run_sample.py` 对 AAPL 固定样例执行完整流程，并输出 `outputs/aapl_market_signal.xlsx`。样例故意包含重复与无效记录，用于验证质量统计并覆盖异常数据处理路径。

### 验证结果

- Skill 结构校验通过：`quick_validate.py` 返回 `Skill is valid!`。
- Python 语法编译通过：主脚本、样例脚本和测试脚本均可编译。
- 自动化测试通过：覆盖行情清洗、新闻清洗、请求范围过滤、无效时间范围和固定样例端到端 Excel 生成；测试会检查工作表、行数、质量统计和 Excel 核心属性已移除。
- 固定样例端到端运行通过：生成的工作簿包含 5 条清洗后行情记录和 2 条清洗后新闻记录；行情与新闻各识别出 1 条重复记录和 1 条无效记录。
- 在线数据接入验证通过：使用 Twelve Data 与 Yahoo Finance RSS 完成 AAPL 单股票采集，获得 25 条清洗后行情记录和 14 条清洗后新闻记录。
- 阶段一限制已确认：不包含财务报表、公告、情感打分、价格预测、多股票比较或定期任务；这些能力保留给后续阶段。

### 阶段产出

- 可被 Agent 发现和调用的 Skill 基础结构。
- 一套最小输入/输出契约和数据字段定义。
- 一个可以重复运行的单股票综合分析样例。
- 一份包含行情、新闻或舆情数据的基础 Excel 文件。

## 阶段二：数据治理与综合指标

### 完成状态

已完成并验证；本轮已扩展为面向中国 A 股、B 股、港股和美股的多市场版本。

### 工作事项

- 支持中国 A 股、B 股、港股和美股的代码解析、市场识别和交易币种记录。
- 为中国市场接入行情、新闻、财务报表和公告数据；保留美股 Twelve Data、Yahoo Finance RSS 和 SEC EDGAR 路径。
- 将中国 A 股、B 股和港股财务数据统一为收入、净利润、资产、负债、流动资产、流动负债、现金及经营现金流等通用指标。
- 让中文新闻进入现有的可解释舆情分类、范围过滤、去重和质量统计流程。
- 在 Excel 的主体、行情、财务、新闻、公告、来源和质量工作表中增加市场与币种字段。
- 对中国接口加入 AKShare 本地缓存、接口备用路由和不支持能力的明确记录。

### 完成方式

1. **市场解析**：新增 `--market` 参数和统一代码解析器，支持 `600519`、`900901`、`200002.SZ`、`00700.HK`、`AAPL` 等输入；自动识别交易所，校验冲突后缀，并记录标准代码、数据源代码、市场名称和币种。B 股按交易所区分上海 USD、深圳 HKD。
2. **中国行情和新闻**：使用 AKShare 接入 A 股、B 股和港股行情；A 股和港股分别配置备用接口。使用股票新闻接口获取中文标题、内容、发布时间、来源和链接，并复用关键词规则完成中文正面、负面或中性分类。
3. **中国财务数据**：A 股和 B 股调用利润表、资产负债表、现金流量表；港股调用年度利润表、资产负债表、现金流量表和公司资料。通过字段别名将中文财务字段映射到统一指标，并保留报告期、公告或申报日期、报告类型、报表货币和来源。
4. **中国公告与降级边界**：A 股和 B 股尝试接入个股公告接口，统一为 `公告` 记录并保留公告链接；当 B 股接口没有可用结果时记录为未支持；港股暂未配置专用公告接口，运行结果在 `数据来源` 中标记为未支持，不生成猜测内容。
5. **美股路径保留**：继续使用 Twelve Data 获取美股日线行情，使用 Yahoo Finance RSS 获取新闻，使用 SEC EDGAR 获取主体、公司事实和 10-K、10-Q、8-K；美股继续校验 Twelve Data API Key 和 SEC 联系邮箱 User-Agent。
6. **统一治理与 Excel**：扩展清洗器以处理 pandas/AKShare 数据、中文日期、逗号数值和空值；将市场、币种、来源、缓存状态和质量统计写入九张工作表，并保持文档元数据清理规则。

### 验证结果

- 固定样例端到端运行通过：生成九张工作表，包含 5 条行情、10 条财务、2 条新闻、1 条时间范围内公告和 14 条衍生指标记录。
- 财务数据验证通过：8 个配置指标被解析为统一记录，报告期、申报日期、表单、单位和 accession number 均保留。
- 公告数据验证通过：固定样例成功解析 SEC 申报历史，并对日期范围外的申报进行质量计数。
- 数据清洗验证通过：行情、新闻、财务和公告均有独立的原始行数、清洗后行数、重复数、无效数和范围外数统计。
- 来源与缓存规则验证通过：工作簿包含 `数据来源`，API Key 不进入来源 URL；缓存目录已加入 `.gitignore`。
- 指标验证通过：`指标分析` 含行情、财务、新闻和公告指标，并保留计算方法与证据字段。
- 14 项自动化测试通过：覆盖阶段一回归、四类市场代码解析、B 股币种、中文行情与新闻归一化、中文财务指标提取、港股年度报告类型归一化、财务行归一化与报告期间区分、工作表契约、Excel 核心属性移除、SEC User-Agent 校验、URL 脱敏和响应缓存。
- 在线接口验证：阶段一的 Twelve Data 与 Yahoo Finance RSS 在线采集已验证；SEC 在线适配器已按官方接口结构实现，并要求调用者配置带联系邮箱的 User-Agent。使用测试身份完成真实接口响应、gzip 解压、财务事实、申报历史和缓存命中验证；完整在线复核输出 5000 条行情、115 条财务、14 条新闻、40 条公告和 14 条指标，第二次运行的五个来源均命中缓存。部署时应替换为实际维护的联系邮箱。
- 多市场代码验证通过：A 股、B 股、港股和美股均有标准化结果；B 股币种按上海 USD、深圳 HKD 区分；冲突市场后缀会在请求阶段失败。
- 中文适配器离线验证通过：中文行情字段可归一化为 OHLCV，中文新闻可完成关键词分类，A/B 股中文财务字段可解析为 8 个通用指标，报告期和币种字段均保留。
- 贵州茅台在线验证通过：以 `600519` 作为 A 股输入，AKShare 行情、中文新闻、A 股三张财务报表和个股公告接口均完成真实请求验证；行情接口在主路由不可用时可回退到腾讯路由，缓存可在后续运行复用。输出工作簿包含市场、币种、财务报告期间、新闻舆情和数据质量结果。
- B 股和港股路由冒烟验证通过：B 股行情、新闻和财务适配器可运行，B 股公告接口在无可用返回时会记录为未支持；港股行情、新闻、年度财务和主体信息可运行，港股公告保持明确的未配置状态。
- 中国市场边界已确认：AKShare 未安装时会明确提示依赖缺失；港股公告当前会在来源记录中标记为未配置；阶段二仍不包含价格预测。

### 阶段产出

- 覆盖行情、财务、新闻、公告和舆情的统一数据模型。
- 支持中国 A 股、B 股、港股和美股的数据路由及市场币种标识。
- 可追溯的数据来源记录、质量检查结果和缓存机制。
- 包含综合指标、来源说明和质量报告的 Excel 模板。
- 单股票综合分析结果能够支持横向检查和后续复用。

## 阶段三：预测分析

### 完成状态

已完成并验证。正式预测仅允许使用阶段二同次在线采集并清洗后的真实数据。

### 工作事项

- 建立基准模型和至少一种可解释的预测模型。
- 加入时间序列验证、回测、误差指标和数据泄漏检查。
- 输出预测区间、不确定性、实际值对比和模型局限。
- 形成可复用的预测分析流程，避免针对单个股票硬编码。

### 完成方式

1. **真实数据约束**：在主命令中增加 `--forecast`。预测模式强制要求 `online`，并检查行情、财务、新闻和公告来源；任一预测输入包含 `fixture` 来源时立即失败。缓存数据可以使用，但必须来自阶段二在线适配器。
2. **预测目标与特征**：以当前交易日信息预测下一交易日收盘收益率。技术面使用一日/五日收益、五日/二十日均线偏离、二十日波动率和成交量比；舆情面使用近七日新闻数量与情绪差；事件面使用近三十日公告数量；基本面使用净利率、流动比率、经营现金流率、收入变化和财务覆盖率。
3. **防止数据泄漏**：每个样本的价格只截止到特征日；财务记录要求 `filed_date` 不晚于特征日；新闻和公告时间不晚于特征日；每次回测的标准化参数和模型只使用更早样本。多步预测时，未来外部特征固定在最后真实数据截止点，不生成未来新闻或财务数据。
4. **模型建立**：实现 `last_close_baseline/persistence-v1` 基准模型和 `multisignal_ridge/ridge-v1` 可解释岭回归。岭回归使用 L2 正则化和确定性最小二乘求解，不依赖随机切分；预测收益率按历史分布设置稳定性边界。
5. **走步回测与选择**：实现扩展窗口单步走步验证，为两个模型逐日保存预测收盘价和实际收盘价，计算 MAE、RMSE、MAPE、收益率 MAE 和上涨/持平/下跌三分类方向准确率。以 RMSE 较低者为入选模型，MAE 作为并列判定依据，不强制选择复杂模型。
6. **预测区间与多步结果**：使用回测绝对误差的 90% 分位数建立经验预测区间，并按预测步数平方根扩大区间。多步预测递归使用前一步预测价格，输出数据截止日、预计交易日、点预测、上下界、模型版本和输入状态。
7. **Excel 交付**：在预测运行中新增 `预测结果`、`模型评估`、`回测明细`、`特征贡献` 四张工作表。原有九张阶段二工作表保持不变；未请求预测时不会生成预测工作表。
8. **Skill 与契约**：新增 `references/forecasting.md`，同步更新 `SKILL.md`、`README.md`、数据契约、依赖、调用元数据和自动化测试。

### 验证结果

- 21 项自动化测试通过，并在将 Python 运行时警告视为错误的条件下复核通过。
- 测试覆盖固定夹具禁止预测、预测最小历史数据、财务申报日可用性、真实来源检查、两个模型、扩展窗口回测、预测区间、特征贡献和十三张预测工作表。
- Skill 语法、Python 编译、依赖完整性、Excel 工作表结构和 OOXML 核心属性清理均完成验证。
- 贵州茅台真实在线端到端预测通过：阶段二获得 649 条行情、820 条财务、10 条新闻和 223 条公告，构建 628 个监督样本，并使用最后 40 个样本执行走步验证。
- 真实回测没有强行选择复杂模型：上一收盘价基准模型的 MAE、RMSE 和 MAPE 分别约为 14.656、21.821 和 1.117%，低于本次岭回归的约 16.435、23.332 和 1.253%，因此主要预测选择基准模型，同时保留岭回归结果和特征贡献供复核。
- 五步预测为两个模型各输出五行，共十行结果；工作簿包含点预测、90% 经验区间、回测实际值对比、模型选择依据和数据截止信息。
- 预测边界已验证：未来交易日仅排除周末，尚未接入交易所节假日日历；多步预测的未来新闻、公告和财务输入固定在最后真实数据截止点；预测结果不构成投资建议。

### 阶段产出

- 基准模型、预测模型和统一的模型配置方式。
- 包含验证过程、误差指标和回测结果的预测报告。
- 包含点预测、不确定性、实际值对比和局限说明的 Excel 工作表。
- 可迁移到不同股票、行业或主题的预测分析流程。
- 一份基于真实阶段二数据生成的 `outputs/maotai_forecast.xlsx` 示例报告。

## 阶段四：稳定性与可扩展性

### 完成状态

已完成并验证。阶段四保留单股工作流，同时增加显式多主体清单、失败恢复、运行日志、行情交叉校验、重复执行状态和组件版本管理。

### 工作事项

- 支持多股票、行业、主题和定期任务。
- 增加多数据源交叉校验、失败恢复和运行日志。
- 建立自动化测试、样例数据集和端到端验收流程。
- 对数据源、模型、Excel 模板和 Skill 指令进行版本管理。

### 完成方式

1. **多主体任务契约**：新增 `scripts/market_batch.py` 和批量契约版本 `1.0`，支持 `portfolio`、`industry`、`theme` 三类清单；阶段五将其升级为批量契约版本 `2.0` 以承载模型选择参数。每个清单必须显式列出股票代码、市场和可选分组，不自动推测行业或主题成分股；单次最多处理 50 个去重后的主体。
2. **横向比较交付**：批量运行复用阶段二、阶段三的单股完整流程，为每个主体生成独立工作簿，并汇总最新收盘价、区间收益率、收益波动率、净利率、流动比率、经营现金流率、新闻情绪、数据覆盖、预测状态、入选模型、下一步预测区间和交叉校验状态。批量工作簿包含 `README`、`主体任务`、`横向比较`、`版本信息`。
3. **失败恢复与隔离**：实现逐主体完整流程重试，重试次数限制为 1 至 5。每次尝试记录成功或失败；开启 `continue_on_error` 后，单个主体失败不会删除其他主体结果，批量状态区分 `pass`、`warning`、`partial` 和 `failed`。
4. **结构化运行日志**：新增 JSONL 日志，使用随机运行标识和事件序号记录任务、主体、市场、尝试次数、耗时、错误类型和结果。日志不记录 API Key，并沿用单股流程的 URL 脱敏规则。
5. **可重复任务入口**：清单支持 `schedule.interval_hours` 和状态文件。外部调度器可以重复调用同一命令；未到间隔时返回 `skipped`，`--force` 用于人工立即执行。输出文件名保持稳定，不加入运行日期。
6. **行情多源交叉校验**：单股和批量任务均可启用交叉校验。A 股根据主行情来源从东方财富、腾讯和新浪 AKShare 路由中选择独立备用源；港股使用两条已配置行情路由；B 股和美股明确记录未配置。校验按交易日对齐收盘价，报告重叠数量、平均差异、最大差异和超容差行数，不修改主数据。
7. **集中版本管理**：新增 `scripts/versioning.py`，统一维护 Skill、单股数据契约、批量契约、数据源适配器、预测引擎、Excel 模板和两个模型的版本。所有单股和批量工作簿新增 `版本信息` 工作表。
8. **验收与文档**：新增 `scripts/run_acceptance.py`、`tests/test_stage_four.py`、`examples/china_liquor.yaml`、批量契约和运行规范，同步更新 `SKILL.md`、`README.md`、调用元数据和单股数据契约。

### 验证结果

- 31 项自动化测试通过，并在将 Python 运行时警告视为错误的条件下复核通过。
- 测试覆盖清单版本、重复主体、参数校验、多主体执行、重试后恢复、失败隔离、警告保留、调度间隔、行情交叉校验、版本注册、单股兼容性和 Excel 核心属性清理。
- 确定性端到端验收通过：固定 AAPL 数据完成单股工作簿、批量入口、结构化日志和四张批量汇总工作表验证。
- 白酒行业真实在线任务通过：贵州茅台和五粮液均在第一次尝试完成行情、财务、新闻、公告、预测和 Excel 输出，批量状态为 `pass`，失败主体为 0。
- 贵州茅台获得 649 条行情、820 条财务、10 条新闻和 223 条公告；五粮液获得 649 条行情、879 条财务、10 条新闻和 276 条公告。两个主体均完成五步、双模型预测。
- 两个主体的行情交叉校验均获得 649 个重叠交易日，主行情腾讯路由与备用新浪路由的收盘价平均差异、最大差异和超容差行数均为 0，校验状态为 `pass`。
- 重复调用真实行业清单时，间隔状态验证返回 `skipped`，说明状态文件能够阻止未到周期的重复运行。
- 两份单股行业工作簿各包含 15 张工作表，批量汇总工作簿包含 4 张工作表；所有检查的 Excel 文件均不包含 OOXML 创建或生成时间核心属性。
- Skill 结构校验、Python 编译、依赖完整性、清单 dry-run、缓存复用和差异格式检查均通过。

### 阶段产出

- 支持多主体和多类型任务的完整工作流。
- 具备失败恢复、日志记录和数据源交叉校验能力的运行框架。
- 覆盖核心逻辑、数据契约和端到端流程的测试体系。
- 可持续维护的数据源、模型、模板和 Skill 版本管理机制。
- 一份真实白酒行业横向比较报告 `outputs/china_liquor_batch.xlsx`，以及贵州茅台和五粮液独立明细报告。

## 阶段五：预测模型选择与可信度优化

### 完成状态

已完成并验证。

### 阶段背景

阶段三和阶段四已经能够基于真实在线数据训练并比较上一收盘价基准模型与多信号岭回归模型。当前真实白酒行业样例中，基准模型的价格误差低于岭回归，因此系统选择了基准模型，导致预测收盘价与前一收盘价相同。这属于模型选择结果，不是计算错误，但说明当前模型体系对涨跌变化的识别能力仍需要进一步验证和增强。

本阶段的核心目标不是强制让预测价格发生变化，而是建立更合理、更透明、更稳定的模型选择机制，只有当复杂模型在严格回测中真正改善结果时，才将其作为主要预测模型。

### 工作事项

- 扩充可比较的基准模型和可解释预测模型集合。
- 将价格误差、收益率误差、涨跌方向和稳定性纳入模型选择。
- 区分“收盘价预测准确度”和“涨跌方向判断能力”两个目标。
- 增加不同市场状态、不同时间窗口和不同预测步长下的稳健性检验。
- 加入交易成本、滑点和换手率等研究辅助指标，避免只根据价格误差评价模型。
- 输出模型选择理由、相对基准改善幅度、模型不确定性和未通过选择的原因。
- 保持所有评估遵守阶段二真实数据来源和阶段三时间可用性约束。

### 完成方式

1. **模型候选集合**：在 `scripts/forecasting.py` 中保留 `last_close_baseline`，增加 `moving_average_baseline`、`historical_mean_baseline`、`exponential_smoothing_baseline`，并继续使用 `multisignal_ridge`；五个模型使用统一的特征、目标和回测边界。
2. **统一评估指标**：对每个模型计算价格 MAE、价格 RMSE、MAPE、收益率 MAE、收益率偏差、价格偏差、三分类方向准确率、方向 F1、经验区间覆盖率和区间宽度。
3. **双目标模型选择**：使用可配置的价格权重和方向权重生成综合评分，默认权重为 0.65 和 0.35；基准模型始终保留为最低性能门槛。
4. **基准改善门槛**：岭回归必须满足价格 RMSE 或方向准确率的最小改善条件，同时不能超过价格和方向恶化上限；每个候选模型都记录 `eligible_for_selection`、综合评分和 `selection_reason`。
5. **稳健性验证**：增加 20、40、80 样本验证窗口，以及完整窗口、低波动和高波动切片；结果写入 `模型稳健性`，并作为模型入选约束。
6. **交易可实现性评估**：将方向转换为研究信号，在 `no_cost`、`slippage_only` 和 `base_cost` 三种场景下计算累计毛收益、累计净收益、最大回撤、胜率、换手率、交易次数和成本。
7. **防止选择偏差**：模型参数、特征处理、选择权重和门槛均在同一验证设计中固定；走步训练只使用目标日前的样本，未来财务、新闻和公告输入固定在真实数据截止点。
8. **预测结果交付**：扩展 `预测结果`、`模型评估` 和 `回测明细`，并新增 `模型稳健性`、`成本敏感性`；工作簿保留全部候选模型、方向、评分、门槛、成本假设和选择理由。
9. **真实数据验证**：使用贵州茅台和五粮液的真实在线阶段二数据完成批量端到端验证，并使用可控的可预测信号测试场景验证复杂模型确实能够在满足门槛时被选择。
10. **Skill 与契约同步**：已同步更新 `references/forecasting.md`、`references/data_contract.md`、`references/batch_contract.md`、`README.md`、`SKILL.md`、版本注册表、批量示例、验收入口和自动化测试。

### 预期验证结果

- 34 项自动化测试全部通过，覆盖五模型候选、综合评分、改善门槛、多窗口验证、方向指标、成本敏感性、选择理由、时间可用性、批量参数和 Excel 契约。
- 确定性端到端验收通过，批量汇总仍保持 `README`、`主体任务`、`横向比较`、`版本信息` 四张工作表。
- 贵州茅台真实在线验证通过：649 条行情、820 条财务、10 条新闻、223 条公告，五模型完成评估，选择 `historical_mean_baseline`；岭回归因至少一个稳健性窗口未通过而未入选。
- 五粮液真实在线验证通过：649 条行情、879 条财务、10 条新闻、276 条公告，五模型完成评估，选择 `historical_mean_baseline`；行情交叉校验状态为 `pass`。
- 两个真实主体均生成 25 行预测结果、5 行模型评估、稳健性结果、15 行成本敏感性结果和 15 行岭回归特征贡献；单股工作簿均包含 17 张工作表。
- 可控的可预测信号测试场景中，`multisignal_ridge` 同时满足价格改善和方向改善门槛并被选中，证明复杂模型不是被固定排除。
- 贵州茅台和五粮液的真实结果均保留实际选择，不人为强制岭回归胜出；选中模型的第一步预测分别为 1329.7395 和 71.9251。
- 所有检查的预测工作簿均不包含 OOXML 创建或生成时间核心属性，且保持财务申报日、新闻发布时间和公告日期的可用性约束。

### 阶段产出

- 可配置的五模型比较和价格/方向双目标选择引擎。
- 包含价格目标、方向目标、相对基准改善和选择理由的模型评估结果。
- 包含稳健性窗口和交易成本敏感性分析的预测报告。
- 更新后的 Excel 预测模板、数据契约、批量契约、Skill 说明和自动化测试。
- 基于贵州茅台和五粮液真实中国股票数据的模型选择验证报告。

## 阶段六：机器学习超额收益预测与严格样本外验证

### 完成状态

已完成并验证。

### 阶段背景

阶段五已经建立五模型比较、价格与方向双目标评分、多窗口稳健性验证和交易成本敏感性分析，但当前模型仍主要使用单只股票的历史数据预测下一交易日收益率，并进一步换算为预测收盘价。

个股日收益率包含大量不可预测的信息冲击，增加更复杂的算法并不必然提高预测能力。本阶段不以“机器学习模型必须胜出”为目标，而是将主要预测对象调整为相对市场或行业基准的超额收益，建立多股票面板、独立测试集和严格的模型准入机制。机器学习模型作为接受检验的候选模型，不作为系统预设的最终答案。

### 工作目标

- 将主要预测目标从具体股价调整为未来个股超额收益率。
- 输出个股未来跑赢或跑输基准的概率，以及股票横截面预测排名。
- 建立适合机器学习训练的多股票时间截面面板数据。
- 引入数量有限、可解释且可重复训练的机器学习候选模型。
- 建立训练、参数选择、模型选择和最终评价相互隔离的验证体系。
- 检查预测结果在不同股票、行业、市值和市场状态下的稳定性。
- 将交易成本、滑点、换手率和最大回撤纳入正式评价。
- 保留简单模型，允许机器学习模型因表现不足而不被采用。

### 实施范围

首轮优先针对中国 A 股实施，验证稳定后再迁移到 B 股、港股和美股。首轮主要预测未来 1 日和未来 5 日个股超额收益率，并输出未来跑赢基准的概率。

超额收益率定义为：

```text
个股超额收益率 = 个股复权收益率 - 基准指数收益率
```

具体股价预测降为辅助结果，不再作为机器学习模型的主要评价目标。基准指数必须由用户明确指定，或通过公开、固定且可追溯的市场映射规则确定。

### 工作事项

1. **预测目标契约**：新增未来 1 日、未来 5 日超额收益率回归目标和跑赢基准分类目标。每个任务记录预测周期、基准指数、信息截止点、目标交易日和标签计算方法。
2. **基准指数契约**：支持用户指定市场指数或行业指数。系统不得在未记录规则的情况下自动更换基准；每条预测保留基准代码、基准类型和基准行情来源。
3. **复权行情处理**：使用复权价格计算个股收益率，识别分红、送转、拆股和异常价格跳变，避免公司行为被模型错误识别为可预测信号。
4. **多股票面板数据**：建立以 `股票代码 + 特征日 + 目标日` 为主键的面板数据，包含个股行情、基准行情、行业信息、财务数据、新闻公告、数据质量状态和标签。
5. **历史主体完整性**：保存股票上市、停牌、退市和历史指数成分信息，禁止只使用当前仍在交易或当前仍属于指数的股票构造历史训练集，降低生存者偏差。
6. **时间可用性控制**：价格特征只能使用特征日及以前的数据；财务信息以实际公告日为可用边界；新闻公告以实际发布时间为边界；目标收益率只能用于模型训练标签和事后评价。
7. **特征体系扩展**：增加个股动量、短期反转、波动率、振幅、换手率、流动性、相对市场动量、滚动 Beta、市场相关性、行业强度、财务横截面排名和新闻事件特征。
8. **特征处理约束**：缺失值填补、异常值处理、标准化和特征筛选只能使用对应训练窗口拟合；预测窗口不得使用完整数据集计算的处理参数。
9. **基准模型保留**：继续保留零超额收益、历史平均、岭回归和 Elastic Net。所有机器学习模型必须在完全相同的样本、目标和时间边界下与基准模型比较。
10. **有限机器学习模型**：首轮引入 Elastic Net、随机森林，以及 XGBoost 或 LightGBM 中的一种梯度提升树模型。首轮不引入 LSTM、Transformer、强化学习和大规模深度神经网络。
11. **嵌套时间验证**：内部训练集用于拟合模型，内部验证集用于选择参数，外部滚动测试集用于模型比较，并保留一段完全不参与参数和模型选择的最终测试集。
12. **重叠标签处理**：未来 5 日等多步收益目标存在时间重叠时，清除训练集和验证集边界附近的重叠样本，避免目标信息间接泄漏。
13. **统一模型评价**：回归任务计算 MAE、RMSE、样本外 R²、IC 和 Rank IC；分类任务计算方向准确率、F1、AUC、Brier Score 和概率校准误差。
14. **经济价值评价**：根据预测超额收益执行分组检验，计算高预测组与低预测组收益差、胜率、换手率、最大回撤，以及无成本、滑点和基础成本后的结果。
15. **模型准入机制**：机器学习模型必须在多数外部测试窗口中击败简单基准，在最终测试集保持改善，并且成本后结果没有完全消失，才允许成为正式候选模型。
16. **模型解释**：输出全局特征重要性、局部预测解释和不同窗口的重要性稳定性。特征贡献只解释模型行为，不表述为因果关系。
17. **模型状态管理**：模型状态分为 `experimental`、`research_only`、`candidate` 和 `selected`。新模型默认处于实验状态，不得直接替换阶段五模型。
18. **Excel 交付扩展**：新增机器学习预测、模型排行榜、滚动验证、特征重要性、预测解释、分组检验和数据泄漏检查等工作表。
19. **版本与可重复性**：记录数据契约、特征版本、模型版本、参数配置和随机种子。相同数据快照与配置应产生一致结果。
20. **真实数据验证**：选取多个行业、不同市值和不同波动特征的 A 股构建面板，验证模型在上涨、下跌和震荡环境中的表现。

### 完成方式

1. **预测目标契约**：新增 `scripts/ml_forecasting.py`，统一生成未来 1 日和未来 5 日的 `stock_return`、`benchmark_return`、`excess_return` 与 `outperformed`；每条样本保留股票代码、特征日、目标日、预测周期和基准代码。
2. **基准指数契约**：新增 `scripts/market_ml.py` 的 `benchmark` 清单字段和 `fetch_online_china_index_prices`；运行时只使用清单明确给出的指数代码，不自动替换基准，并把基准来源写入面板和 `数据来源`。
3. **复权行情处理**：新增 `fetch_online_china_adjusted_prices`，优先请求 AKShare 前复权 `qfq` 行情；每条面板样本记录 `adjustment=qfq`，数据泄漏检查会拒绝未使用前复权行情的标签。
4. **多股票面板数据**：`build_excess_return_panel` 以股票、特征日、目标日和周期构造面板，融合阶段二财务、新闻、公告数据以及行业、市值分组字段；最新预测统一使用全体股票和基准共同可用的特征日。
5. **历史主体完整性**：清单要求显式填写 `membership_policy`，并保留上市、退市或历史成分记录字段；固定用户股票池可运行，但没有历史成分资料时会在 `数据泄漏检查` 中明确给出生存者偏差警告，不把警告伪装成通过。
6. **时间可用性控制**：面板在每个特征日调用阶段五的点时特征构造逻辑；财务按 `filed_date`、新闻按发布时间、公告按 `filed_date` 截止，目标收益只进入标签和事后评价。
7. **特征体系扩展**：新增股票与基准动量、相对动量、滚动 Beta、相关性、相对波动率、振幅、回撤、行业与市值指示变量，以及同一特征日的横截面排名。
8. **特征处理约束**：岭回归、Elastic Net、随机森林和梯度提升树都在训练窗口内拟合缺失值填补和标准化；模型解释使用训练中位数和训练集置换重要性，不读取最终测试集处理参数。
9. **基准模型保留**：保留零超额收益、历史均值和面板 Ridge，并与 Elastic Net、随机森林、直方图梯度提升在同一面板、同一标签和同一时间边界下比较。
10. **有限机器学习模型**：引入 Elastic Net、Random Forest 和 scikit-learn 内置 `HistGradientBoostingRegressor`；采用内置梯度提升树而不是外部 XGBoost/LightGBM，避免额外 OpenMP 系统依赖，仍完成非线性梯度提升候选的检验。
11. **嵌套时间验证**：内部窗口只选择有限参数，外部滚动窗口锁定候选，最终留出集只执行接受或拒绝；每个拆分都按时间排序并使用清除边界重叠的 purged 规则。
12. **重叠标签处理**：`_purged_split` 要求训练标签结束于验证特征日之前，未来 5 日标签不会与验证窗口在边界处共享信息。
13. **统一模型评价**：`_metrics` 输出 MAE、RMSE、样本外 R²、IC、Rank IC、方向准确率、F1、AUC、Brier Score 和校准误差，全部写入 `模型排行榜` 与 `滚动验证`。
14. **经济价值评价**：按预测超额收益构造高组减低组研究组合，在无成本、仅滑点和基础成本场景下计算累计收益、平均价差、胜率、换手率和最大回撤。
15. **模型准入机制**：机器学习模型必须多数外部窗口优于最佳简单模型、多数窗口 Rank IC 为正、最终留出集优于简单模型、成本后价差为正且无数据治理警告，才可标记为 `selected`。
16. **模型解释**：线性模型输出系数，树模型输出内置或训练集置换重要性；最新预测输出将单一特征替换为训练中位数后的局部预测差异，明确标注为模型关联而非因果关系。
17. **模型状态管理**：每个周期的模型在 `模型排行榜` 中标记为 `research_only`、`candidate` 或 `selected`，并保留锁定候选和拒绝原因；固定股票池的生存者偏差警告会阻止机器学习模型晋级为正式选中模型。
18. **Excel 交付扩展**：新增 `面板样本`、`机器学习预测`、`模型排行榜`、`滚动验证`、`特征重要性`、`预测解释`、`分组检验`、`成本敏感性`、`数据泄漏检查`、`最终测试明细`、`参数搜索`、`数据来源` 和 `模型版本`。
19. **版本与可重复性**：在 `scripts/versioning.py` 中新增阶段六契约、特征、引擎和六类模型版本；固定随机种子为 `1729`，参数搜索、模型状态和最终测试用途均写入工作簿。
20. **真实数据验证**：使用贵州茅台、招商银行、美的集团、宁德时代、恒瑞医药和海康威视六只 A 股，以沪深 300 为显式基准运行真实在线面板；覆盖食品饮料、银行、家用电器、电力设备、医药生物和计算机行业。

### 首轮候选模型

| 模型 | 定位 |
| --- | --- |
| 零超额收益基准 | 假设个股未来表现与基准一致，是最低预测基准。 |
| 历史均值基准 | 使用训练窗口中的历史平均超额收益率。 |
| Ridge | 保留阶段五的可解释正则化线性模型。 |
| Elastic Net | 同时使用 L1 和 L2 正则化进行变量收缩与筛选。 |
| Random Forest | 检验非线性关系和变量交互。 |
| XGBoost 或 LightGBM | 选择其中一种作为首轮梯度提升树候选，避免同时测试大量相似模型。 |

### 分阶段实施

1. **阶段六 A：数据与验证基础**：完成复权行情、基准指数、超额收益目标、多股票面板、历史主体完整性、时间可用性检查和独立最终测试集。即使没有机器学习模型胜出，这部分也必须能够独立交付。
2. **阶段六 B：有限模型实验**：加入 Elastic Net、随机森林和一种梯度提升树，使用预先固定的有限参数范围进行内部参数选择。保留所有模型的完整结果，不只展示表现最好的模型。
3. **阶段六 C：影子验证**：机器学习模型先以 `research_only` 状态运行。只有通过多窗口、最终测试集、跨行业和成本后验证，才能升级为 `candidate` 或 `selected`。

### 模型入选条件

- 不存在目标泄漏、时间泄漏和可识别的生存者偏差。
- 样本外表现优于零超额收益和历史均值基准。
- IC 或 Rank IC 在多数滚动窗口中保持方向一致。
- 跑赢概率具备基本的样本外校准能力。
- 模型优势不依赖少数极端样本或单一行业。
- 在不同行业、市值和市场状态下没有严重失效。
- 加入交易成本和滑点后仍保留研究价值。
- 最终测试集没有被用于重新选择参数、特征或模型。
- 固定数据、参数和随机种子后能够重复产生一致结果。

未满足准入条件时，系统继续选择简单基准模型，并明确记录机器学习模型未通过的原因。

### Excel 工作表

| 工作表 | 内容 |
| --- | --- |
| `机器学习预测` | 超额收益率、跑赢概率、预测排名和预测区间。 |
| `模型排行榜` | 全部模型的样本外指标、状态和选择理由。 |
| `滚动验证` | 各外部测试窗口的模型表现。 |
| `特征重要性` | 全局特征贡献和跨窗口稳定性。 |
| `预测解释` | 主要预测结果的局部解释。 |
| `分组检验` | 不同预测分组的实际超额收益。 |
| `成本敏感性` | 成本、滑点、换手率和最大回撤分析。 |
| `数据泄漏检查` | 特征时间、标签时间、历史主体和异常记录。 |
| `模型版本` | 模型、特征、参数和数据契约版本。 |

### 预期验证结果

- [x] 超额收益标签能够由复权个股行情和基准行情正确生成。
- [x] 同一目标日的所有模型使用相同训练范围和可用信息。
- [x] 普通随机 K-Fold 被禁止，所有验证保持时间顺序。
- [x] 至少覆盖一个简单基准胜出的测试场景；真实 A 股的 1 日目标选择零超额收益基准，5 日目标选择面板 Ridge。
- [x] 至少构造一个机器学习能够识别非线性信号的确定性测试场景；树模型在该场景通过外部窗口检验并被选中。
- [x] 真实数据中没有机器学习模型满足全部晋级条件时如实记录，未人为调整选择结果。
- [x] 最终测试集与参数选择、特征选择和模型选择过程隔离，并独立写入 `最终测试明细`。
- [x] 成本后价差不满足门槛或数据治理存在警告时，机器学习模型不会标记为正式入选。
- [x] 输出模型入选、保留或拒绝原因，并保留全部候选模型结果与参数试验。
- [x] 工作簿清除 OOXML 文档创建、更新和生成时间核心属性；业务交易日、报告期和数据截止日继续保留。

### 实际验证记录

- `tests/test_stage_six.py`：4 项阶段六测试通过，覆盖清单和显式基准、前复权超额收益标签、统一特征日、非线性模型可被选中、工作簿工作表和核心日期元数据清除。
- 全量 `unittest` 回归测试：阶段一至阶段五既有测试与阶段六测试全部通过。
- 阶段六确定性非线性场景：`hist_gradient_boosting` 在外部窗口中满足多数窗口改善和正 Rank IC 条件，并通过最终测试接受检查；验证没有固定排除机器学习模型。
- 真实 A 股在线面板：6 只股票、9,936 条面板监督样本、39 个特征、12 条最新预测；工作簿包含 14 张工作表。
- 真实 A 股模型结果：1 日目标选中 `zero_excess`，5 日目标选中 `panel_ridge`；Elastic Net、Random Forest 和 `hist_gradient_boosting` 均完整保留，并按门槛分别记录为研究状态或未入选。
- 真实 A 股数据治理：显式基准、前复权、特征先于目标、点时外部数据、purged 时间切分和禁用随机 K-Fold 均通过；由于示例清单是固定用户股票池，历史成分检查为 `warning`，整体任务状态因此为 `warning`。
- 真实数据输出：`outputs/china_a_ml_panel.xlsx`；没有加入创建日期、更新日期、生成日期或日期化文件名。

### 阶段产出

- [x] 多股票、基准指数和行业信息组成的时间截面面板数据。
- [x] 未来 1 日和未来 5 日超额收益率标签。
- [x] 跑赢基准概率与股票横截面预测排名。
- [x] 有限机器学习模型候选集合。
- [x] 嵌套滚动验证与独立最终测试框架。
- [x] 模型准入、降级和拒绝机制。
- [x] 可解释性、成本敏感性和数据泄漏检查报告。
- [x] 更新后的预测工作簿、数据契约、Skill 说明和自动化测试。
- [x] 基于真实 A 股面板数据的机器学习验证报告。

### 阶段边界

- 不承诺机器学习一定优于简单模型。
- 不将预测结果表述为投资建议或收益保证。
- 不使用当前成分股代替历史成分股。
- 不使用原始未复权价格构造收益标签。
- 不使用最终测试集调整模型、参数或特征。
- 不因模型复杂度更高而优先选择。
- 首轮不实现自动选股、实盘交易和自动下单。
- 首轮不引入深度学习、强化学习或大规模模型搜索。

## 阶段推进规则

- 每个阶段先完成核心工作事项，再确认阶段产出是否可运行、可追溯和可复现。
- 数据源不可用、数据质量不达标或预测条件不成立时，应记录问题并执行降级方案，不以不可靠结果替代缺失能力。
- 新增能力必须同步更新 `SKILL.md`、数据字典、测试和相关输出说明。
- 所有生成文件遵循仓库规则，不添加创建、更新或生成日期标签。
